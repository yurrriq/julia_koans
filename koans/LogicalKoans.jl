module LogicalKoans

not(x) =
  !x

equals(x, y) =
  x ≡ y

not_equals(x, y) =
  x ≠ y

less_than(x, y) =
  x < y

greater_than(x, y) =
  x > y

less_than_or_equal_to(x, y) =
  x ≤ y

greater_than_or_equal_to(x, y) =
  x ≥ y

end
