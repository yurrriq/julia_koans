module ArithmeticKoans

sum(x, y) =
  x + y

difference(x, y) =
  x - y

multiplication(x, y) =
  x * y

division(x, y) =
  x / y

integer_division(x, y) =
  x ÷ y

power(x, y) =
  x^y

modulo(x, y) =
  x % y

end
